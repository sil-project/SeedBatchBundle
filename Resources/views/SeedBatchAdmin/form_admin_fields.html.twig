{% extends 'SonataAdminBundle:Form:form_admin_fields.html.twig' %}

{% block form_widget_simple %}
    {% if form.vars.name == 'code' %}
        {% set attr = attr|merge({'class': attr.class|default('') ~ ' seedbatch-code'}) %}
    {% endif %}

    {{ parent() }}

    {% if form.vars.name == 'code' %}
        {% include('LibrinfoSeedBatchBundle:SeedBatchAdmin:code_link.html.twig') %}
    {% endif %}
{% endblock form_widget_simple %}






